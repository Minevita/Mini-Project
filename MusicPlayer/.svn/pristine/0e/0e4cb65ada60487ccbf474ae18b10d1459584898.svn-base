package MusicPlayerUtil;

import MusicPlayerService.MusicPlayerService;
import MusicPlayerService.MusicPlayerServiceImpl;
import MusicPlayerUtil.*;
import static MusicPlayerUtil.MusicPlayerCommon.*;

public class PlayerOption {
	MusicPlayerServiceImpl mps;
	PageViewer pv = new PageViewer(mps);
	
	/**
	 * allListviewer의 표시 갯수를 바꾸는 메서드 입니다.
	 * @author 김문수
	 * 2021-08-10
	 */
	public void printPerPage(int num) {
		pv.setPrintNum(num);
	}
}
